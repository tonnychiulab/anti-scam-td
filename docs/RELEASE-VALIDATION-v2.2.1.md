# v2.2.1 Hardening 發行驗證報告

- 驗證日期：2026-07-15
- 分支：`agent/v2.2.1-hardening`
- 基準：`origin/main` @ `69b2b9a`（v2.2.0）
- 狀態：通過；目前保留為未提交、未推送變更

## 修補範圍

本版只處理獨立審查列出的四項 P1，不改動戰鬥平衡、關卡、制裁技或既有操作流程。

| 項目 | 結果 |
|---|---|
| Service Worker 預快取容錯 | `cache.addAll` 改為逐檔 `cache.add` 搭配 `Promise.allSettled`；單檔失敗會警告，但不使整次安裝失敗 |
| 排行榜寫入淨化 | 名稱、分數、關卡與時間欄位在寫入前統一轉為安全範圍內的整數／字串 |
| 動態多語字串轉義 | 建塔選單塔名與圖示、排行榜空白訊息及關卡標籤在進入 HTML 模板前轉義；結算提示插值亦補上轉義 |
| 多語 HTML 信任政策 | 新增 `docs/SECURITY-I18N.md`，明列允許 HTML 的欄位、標籤及禁止內容 |

## 自動回歸

- v2.2.0 修改前基準：44 / 44 通過。
- v2.2.1 修改後：47 / 47 通過。
- 新增檢查涵蓋：
  - 排行榜寫入 `Infinity`、`NaN`、負數與超出關卡上限時的淨化。
  - 建塔選單、排行榜與結算純文字插值的 HTML escape。
  - 四語受信任 HTML 白名單、危險標籤、`javascript:`／`data:` 與事件屬性禁令。
  - Service Worker 單一 app-shell 資源失敗時仍完成安裝並呼叫 `skipWaiting()`。
- `git diff --check`：通過，無空白錯誤。

## 真實瀏覽器與 PWA 驗證

使用全新 Chrome 使用者資料目錄與本機 HTTP 來源驗證，未沿用舊快取：

1. 首次開啟完成，畫面版本顯示 `v2.2.1`。
2. Service Worker 成為 active controller，快取名稱為 `asmd-v2.2.1`。
3. 9 個 app-shell 資源全部存在於目前版本快取。
4. 將排行榜空白訊息注入含 `img`／`onerror` 的測試字串後，只呈現文字，未建立惡意節點且未執行事件。
5. 將遊戲分數與關卡設為 `Infinity` 後觸發儲存，實際 localStorage 結果為分數 `0`、關卡 `1`、有效安全整數時間戳。
6. 直接關閉 HTTP 伺服器後重新載入頁面，仍由 Service Worker 快取完整開啟，版本、標題與控制權正確。
7. 線上與離線流程皆未觀察到未捕捉的瀏覽器 JavaScript 例外。

## 結論

獨立審查的四項 P1 已完成對應修補，既有功能回歸與新增安全檢查均通過。此工作區可進入人工差異確認、提交與 PR 階段；本次驗證本身未執行提交、推送、合併或部署。
