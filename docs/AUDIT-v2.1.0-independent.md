# V2.1.0 獨立審查報告（鄰居改動查核）

- 版本：v2.1.0
- 審查日期：2026-07-13
- 審查者：Fable 5（獨立第三方查核，非變更作者）
- 變更作者：Codex（鄰居）
- 審查目的：確認 Codex 從 v2.0.2 改到 v2.1.0 **有沒有亂改、越界改動或夾帶惡意內容**
- 審查基準：`15c477d`（v2.0.2）→ `c7cf5fb`（本機 HEAD）／已部署 `origin/main@9e31119`
- 本次限制：**只讀查核，未修改任何程式、樣式、測試或設定**

---

## 一、總結論

**通過（放行）。查無亂改、查無惡意夾帶。** Codex 的 v2.1.0 改動範圍與其紀錄文件（`RELEASE-VALIDATION-v2.1.0.md`、`CODE-REVIEW-v2.1.0.md`）相符，沒有偷偷動到不該動的地方，也沒有塞入任何可疑程式。33 項自動測試全數通過，版本號四處一致，PC 右鍵防護仍在。

唯一需要處理的是一個**與 v2.1.0 無關的雜項**：工作區裡的 `LICENSE` 檔被刪除（未提交）。詳見第五節。

---

## 二、改動範圍核對

`git diff --stat 15c477d..HEAD`：

| 檔案 | 變動 | 性質 |
|---|---|---|
| game.js | +673 行 | 主要功能（行動導引、二段建塔、支援技能、品質分級） |
| UX-v2.1-mobile-guidance.md | +462 行 | 規格文件（新增） |
| tests/release.test.mjs | +141 行 | 新增回歸測試 |
| style.css | +96 行 | 版面／HUD 樣式 |
| i18n.js | +84 行 | 四語字串 |
| index.html | +67 行 | HUD、更多選單、建塔面板 |
| docs/RELEASE-VALIDATION-v2.1.0.md | +64 行 | 驗證紀錄（新增） |
| README.md | ±12 行 | 版本紀錄／技術重點更新 |
| package.json | 1 行 | 版本號 2.0.2 → 2.1.0 |
| sw.js | 1 行 | 快取版本 v2.0.2 → v2.1.0 |
| .gitignore | +2 行 | 新增 `*.zip`（排除本機備份） |

所有異動都落在「行動 UX + 對應測試 + 文件 + 版本號」這個宣稱範圍內，**沒有出現與 v2.1.0 主題無關的可疑檔案或程式碼**。

---

## 三、惡意／安全夾帶掃描（重點）

對 diff 新增行做高風險樣式全掃（`fetch`、`XMLHttpRequest`、`eval`、`new Function`、`document.write`、`innerHTML`、外部 `http/https/ws` 連結、`import()`、`<script>`、`atob/btoa`、`sendBeacon`、`.src=`）：

- **零命中**——除了兩行 `localStorage`（見下）。
- 無新增任何網路請求、無外連腳本、無 `eval`／動態程式碼、無資料外傳管道。
- `package.json` 未新增任何相依套件，仍為零依賴。

`localStorage` 使用全部為本機、匿名、且以 `try/catch` 包住，無任何個資：

| 鍵 | 內容 |
|---|---|
| `asmd_lang` | 語言偏好 |
| `asmd_ux_v21` | UX 偏好（畫質、減少動態、減少閃光等） |
| 排行榜鍵 | 匿名分數，載入時做型別／範圍消毒 |
| PWA 提示旗標 | 「不再提示」布林值 |

**結論：無惡意夾帶，無隱私外洩風險。**

---

## 四、正確性與一致性查核

- **測試**：`npm test` → **33/33 通過**，0 失敗、0 略過。
- **版本一致性**：`package.json` / `sw.js`（`asmd-v2.1.0`）/ `game.js`（`APP_VERSION='v2.1.0'`）/ `index.html` 版面文字 **四處全為 v2.1.0**，無殘留舊版號。
- **部署一致性**：本機 HEAD 與已部署 `origin/main@9e31119` 檔案內容 **完全相同（diff 為 0）**——線上跑的就是這份程式。
- **PC 迴歸未被破壞**：`cv` 上 `contextmenu → preventDefault` 仍在（右鍵不開瀏覽器選單、不當成左鍵）；鍵盤操作、焦點、Esc 取消流程完整。這正是 v2.0.2 修過的重點，Codex 沒有把它改壞。

---

## 五、需要處理的一個雜項（非 v2.1.0 改動）

**`LICENSE` 檔在工作區被刪除（`git status` 顯示 `D LICENSE`，未 staged、未提交）。**

- `LICENSE`（MIT，Copyright 2026 Tonny Chiu Lab）自 Initial commit 起就存在，**在 HEAD 與線上版本都還在**，所以目前不影響已發布內容。
- Codex 在 `RELEASE-VALIDATION-v2.1.0.md` 已明確聲明：「工作區原有的 LICENSE 刪除不屬於 v2.1.0，本次提交不包含該變更。」——與實際情況相符，**這不是 Codex 亂改，而是既有的工作區殘留**。
- 風險：若之後有人用 `git add -A` 一次加全部再提交，會**誤把 LICENSE 刪掉**。

**建議（你自己決定，我沒有動）**：在提交前執行 `git checkout -- LICENSE` 還原，避免將來誤刪。

---

## 六、與 Codex 自評報告的對照

Codex 自己在 `CODE-REVIEW-v2.1.0.md` 列出 7 項 P2、3 項 P3（手勢取消、支援瞄準取消建塔、強光蓄力跨暫停、減少動態仍震動、音效偏好未保存、建塔無障礙、PWA 版本交接等）。

我複核後認為這份自評**誠實、沒有隱瞞問題也沒有灌水**——它把自己實作的缺口都攤開了。這些屬於「功能未完全達到規格」的體驗性缺口，**不是 bug、不是安全問題、也不是亂改**，可留待 v2.1.1 處理，不影響本版放行。

---

## 七、發行判定

**v2.1.0 可放行。** 鄰居（Codex）沒有亂改：改動範圍正當、無惡意夾帶、無 PC 迴歸、版本一致、測試全過、線上版與原始碼一致。唯一待辦是還原工作區被刪的 `LICENSE`（與本版無關）。體驗細節缺口建議排入 v2.1.1，但不構成阻斷。
